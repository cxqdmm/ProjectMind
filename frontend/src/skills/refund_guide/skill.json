{
  "id": "refund_guide",
  "name": "退款指南",
  "version": "0.1.0",
  "description": "当用户提出退款或退费需求时使用",
  "intents": ["refund", "退费", "退款", "售后"],
  "input_schema": {
    "type": "object",
    "properties": {
      "orderId": { "type": "string", "description": "订单编号，可选" }
    }
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "operation": { "type": "string" },
      "steps": { "type": "array" }
    }
  },
  "permissions": [],
  "examples": [
    {
      "user": "怎么退费？",
      "call": { "provider": "policy", "tool": "operation_guide", "input": { "operation": "退款" } }
    }
  ],
  "dependencies": []
}
